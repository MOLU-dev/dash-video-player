// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: filters.sql

package db

import (
	"context"
)

const getRecommendedFilters = `-- name: GetRecommendedFilters :many
WITH video_info AS (
  SELECT v.channel_id
    FROM videos v
   WHERE v.id = $2
),
tag_matches AS (
  SELECT t.tag AS value,
         'TAG'::text AS type,
         (COUNT(*) * 10)::int AS score,
         t.tag AS key
    FROM videotag vt
    JOIN tag t ON t.id = vt.tag_id
   WHERE vt.video_id = $2
   GROUP BY t.tag
),
channel_matches AS (
  SELECT c.id::text AS key,
         c.name     AS value,
         'CHANNEL'::text AS type,
         10000 AS score
    FROM channels c
    JOIN video_info vi ON vi.channel_id = c.id
),
global_matches AS (
  SELECT c.name     AS value,
         'GLOBAL'::text AS type,
         1 AS score,
         c.name AS key
    FROM categories c
)
SELECT key, value, type
  FROM (
    SELECT key, value, type, score FROM channel_matches
    UNION ALL
    SELECT key, value, type, score FROM tag_matches
    UNION ALL
    SELECT key, value, type, score FROM global_matches
  ) AS all_chips
 ORDER BY score DESC
 LIMIT $1
`

type GetRecommendedFiltersParams struct {
	Limit int32  `json:"limit"`
	ID    string `json:"id"`
}

type GetRecommendedFiltersRow struct {
	Key   string `json:"key"`
	Value string `json:"value"`
	Type  string `json:"type"`
}

// ctx, limit, video_id
func (q *Queries) GetRecommendedFilters(ctx context.Context, arg GetRecommendedFiltersParams) ([]GetRecommendedFiltersRow, error) {
	rows, err := q.db.Query(ctx, getRecommendedFilters, arg.Limit, arg.ID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetRecommendedFiltersRow{}
	for rows.Next() {
		var i GetRecommendedFiltersRow
		if err := rows.Scan(&i.Key, &i.Value, &i.Type); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
